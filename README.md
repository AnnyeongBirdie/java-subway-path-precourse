### 구현 완료
- 메인 메뉴 출력 및 종료(Q) 처리
- 경로 기준 선택(최단 거리 / 최소 시간)
- 잘못된 입력 시 에러 출력 및 재입력
- 출발역 = 도착역 예외 처리
- 역/노선/구간 초기 데이터 설정

### 구현 예정
- 경로 계산 로직(PathService)
- 총 거리 / 총 시간 계산
- validator
- test code


### 다음 작업 (내일)
🔜 경로 탐색 결과 모델 계획

현재는 한 단계 이동 실험을 위해 OneStepResult를 임시 도입
출발역 기준 다음 이동 후보 확인

1-step 도착 여부 판단

향후 경로 탐색 로직 확장 시,
OneStepResult는 PathResult로 발전시킬 예정

전체 경로(역 목록), 총 거리, 총 시간 등을 포함하는 최종 결과 모델로 확장

OneStepResult (현재)
→ PathResult (예정)

현재 단계에서는 알고리즘 구현보다 구조 이해와 흐름 검증을 우선함

현재는 탐색 흐름 확인을 위해 구간을 양방향으로 등록한다. (추후 요구사항에 맞춰 정리)
- DataInitializer 에 헬퍼 추가해서 임시 구현